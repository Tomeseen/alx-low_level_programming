#include <stdio.h>
/**
 * main - code block
 * Description - prints all single digits
 * Return: 0
 */
int main(void)
{
	int num;

	for  (num = 0; num < 10; num++)
		printf("%i", num);
	printf("\n");
	return (0);
}
